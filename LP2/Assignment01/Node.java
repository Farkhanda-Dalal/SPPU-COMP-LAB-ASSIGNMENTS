 // Inner class to represent a node and its level in BFS
class NodeLevel{
    int node;
    int level;

    NodeLevel(int node, int level) {
        this.node = node;
        this.level = level;
    }
}
